# Core (needed for the scoring engine, trade setter and backtest)
pandas>=2.0
numpy>=1.24

# Exchange connectivity (live / paper / fetching klines). REST + websockets.
ccxt>=4.0
# python-binance is an alternative websocket client for the live Mitigator:
# python-binance>=1.0

# Config + secrets
pydantic>=2.0
pydantic-settings>=2.0

# Notifications (entries / exits / circuit-breaker trips)
python-telegram-bot>=21.0

# Coroner (post-trade LLM post-mortem -> human, never into an auto-retune loop)
google-generativeai>=0.7
# Optional deeper-reasoning fallback:
# anthropic>=0.40

# Dev / tests
pytest>=7.0
